# required metadata
sonar.projectKey=1803_MAXVIVA
sonar.projectName=Maxviva
sonar.projectVersion=1.0

# path to source directories (required)
sonar.sources=src/main

# path to test source directories (optional)
sonar.tests=src/test

# path to project binaries (optional), for example directory of Java bytecode
#sonar.binaries=target/classes

# optional comma-separated list of paths to libraries. Only path to JAR file and path to directory of classes are supported.
#sonar.libraries=path/to/library.jar,path/to/classes/dir

# Uncomment this line to analyse a project which is not a java project.
# The value of the property must be the key of the language.
sonar.language=java
sonar.java.binaries=target/classes

# Additional parameters
#sonar.my.property=value

#----- Default SonarQube server
#sonar.host.url=http://172.27.59.40:9000/sonar

#for sonarqube 5.1.1
#sonar.host.url=http://localhost:9000 

#----- Embedded Database
#sonar.jdbc.url=jdbc:h2:tcp://localhost:9092/sonar

#----- MySQL
#sonar.jdbc.url=jdbc:mysql://localhost:3306/sonar4_5_4?useUnicode=true&characterEncoding=utf8&rewriteBatchedStatements=true

#----- Global database settings
#sonar.jdbc.username=sonar
#sonar.jdbc.password=sonar

#----- Default source code encoding
#sonar.sourceEncoding=UTF-8

#----- Security (when 'sonar.forceAuthentication' is set to 'true')
#sonar.login=admin
#sonar.password=admin